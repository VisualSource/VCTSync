[package]
name = "iced-js"
version = "0.1.0"
edition.workspace = true

[dependencies]
rquickjs = { version = "0.14.0", features = [
    "rust-alloc",
    "loader",
    "futures",
    "macro",
    "phf",
] }
iced = { workspace = true, features = ["svg"] }
